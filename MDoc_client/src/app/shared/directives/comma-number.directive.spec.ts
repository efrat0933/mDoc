import { CommaNumberDirective } from './comma-number.directive';

describe('CommaNumberDirective', () => {
  it('should create an instance', () => {
    const directive = new CommaNumberDirective();
    expect(directive).toBeTruthy();
  });
});
